# Prometheus — staging Docker network (§18)
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts/*.yml

scrape_configs:
  - job_name: orion-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - orion-api:5000
