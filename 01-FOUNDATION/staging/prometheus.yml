# Prometheus scrape config — Orion staging (§18)
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: orion-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:5000
